{% extends "base.html.j2" %}

{% block content %}

<div class="row">
    <div class="col">
        <h1>Hello World</h1>
    </div>
</div>

<div class="row">
    <div class="col-6">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>name</th>
                    <th>number</th>
                </tr>
            </thead>
            <tbody>
            {% for record in phone_records %}
                <tr>
                    <td>{{ record.id     }}</td>
                    <td>{{ record.name   }}</td>
                    <td>{{ record.number }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>

{% endblock content %}
